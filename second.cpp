#include<cmath>
#include <algorithm> // Required for max_element and min_element
#include <cstdlib>  // Required for exit()
#include<iostream>
using namespace std;

int main(){
    /*
    int arr[5]={1,2,3,4,5};
    int ma = *max_element(arr,arr+5);
    int am = *min_element(arr,arr+5);
    cout<<ma<<endl<<am;
    return 0;

    int n;
    cin>>n;
    int arr[n];
    int start = 0;
    int last = n-1;
    for (int i = 0;i<n;i++){
        cin>>arr[i];
    }
    while(start<=last){
        swap(arr[start],arr[last]);
        start++;
        last--;
    }
    for(int i = 0;i<n;i++){
        cout<<arr[i]<<" ";
    }
    
    int n;
    cin>>n;
    int arr[n];
    int start = 0;
    int last = 1;
    for (int i = 0;i<n;i++){
        cin>>arr[i];
    }
    while(start<n && last<n){
        swap(arr[start],arr[last]);
        start+=2;
        last+=2;
    }
    for(int i = 0;i<n;i++){
        cout<<arr[i]<<" ";
    }
    */
    
    
}